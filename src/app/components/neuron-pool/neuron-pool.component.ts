import { Component } from '@angular/core';

/**
 * NeuronPoolComponent — stub.
 * Community allocation pools were replaced by the FUN/CUN/SUN bucket system.
 * This component is kept as a placeholder to avoid broken imports elsewhere.
 */
@Component({
  selector: 'app-neuron-pool',
  standalone: true,
  template: ``,
})
export class NeuronPoolComponent {}
